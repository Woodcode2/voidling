# REFUTATION — LANTERN NIGHT establishing shot (Stream D, skeptic to the cinematographer)

Read the same ten images and every cited line myself. Also read what the cinematographer did not: the match reset's camera seed (`prototype3d.ts:6700-6726`), the boot seed (9900-9904), `__warpVoid` (1930-1938), the celestial layer's per-frame re-centre (`island.ts:3658-3661`) and its sprite placement (973-989), the canal light-pool bake (`island.ts:1720-1750`), `bakeContactShadows()` (2766-2795) and the disc tag (`assets3d.ts:85`), the float-lantern and boat drops (`island.ts:5662-5680`), the magnet well (9376-9377), `__matchState` (1941-1960), the bubble show/hide mechanics (`bubbles.ts:261, 294, 393`), the two sibling refutations, and `qa/firstframe.mjs` whole. I re-ran the projection (camOffset (0.62,0.92,0.62), 32° lens, 430/932, camDist 38+262k², the 0.25/0.5 smoothstep) with my own 40-line node script, and I measured pixels in `lantern_u100.png` / `_canvas.png` with pngjs (no browser, no build).

## VERDICT ON THE VERDICT
NO-SHIP is right — a live HUD and a running clock over the establishing shot at the first sampled frame (`lantern_u100.png`) and a title card that, by code, is centred on the lit tower and cannot be gated on this instrument are two majors in the page frame — but the pack's "first frame" is not the first frame: the camera is still swooping in from the spawn at 0.39 s (below), and nobody has photographed what a child actually sees at t=0.

## PER FINDING

### The card and the shot are on two clocks — and the card was in none of these frames
REAL: yes
WHAT I FOUND: `index.html:548` `#titlecard.show { animation: cardFade 4.2s ease forwards; }`, `:558` the keyframes with `72%{opacity:1}`, `prototype3d.ts:5582-5585` `tcEl.classList.remove('show'); void tcEl.offsetWidth; tcEl.classList.add('show'); titleUntil = tClock + 4.6; ... introT = COPY.introLen;`, `:8527` `const dt = Math.min(0.05, dtRaw);`, `:9460` `introT -= dt;`, `:1431-1433` "3.6 still finishes inside the 4.2s title card" — all as quoted. No card in any of the six page frames; `lantern_swing.png` is six HUD frames with no name. The JSON's tPage 0.39 at u100 is ≥ 8 rendered frames (0.05 clamp) and the probe seeds nothing that would hide the card (`firstframe.mjs` strips `.show` from `daily`/`gift` only). 72% × 4.2 = 3.02 s < 3.6 s: on a 60 fps phone the name is leaving for the last 0.58 s of the dive — the 1433 comment is true of the 4.2 s envelope and false of the plateau.
FIX SOUND: yes. Same paused WAAPI animation driven beside `tClock += dt` (8533); delete the CSS animation. No seeded draw, no budget, no art. Carry the Maple/Game Day refuters' note: the probe's `dtRaw` experiment on the owner's phone is what decides whether a child ever sees the drift; the ungateable card is why it stays major.
CORRECTION: none.

### The card is centred on the bathhouse
REAL: yes (by code; invisible in this pack for the reason above)
WHAT I FOUND: `index.html:543-547` `align-items: center; justify-content: center; ... radial-gradient(ellipse 78% 34% at 50% 50%, rgba(9,5,20,0.72) ...)`, `:551` `.name { font-size: clamp(34px, 12vw, 62px)`, `prototype3d.ts:1424` the tagline — as quoted. In `lantern_u100_canvas.png` the tower's six lit tiers span y ≈ 590-1140 px (32%-61%) centred at (50%, 46%); in `lantern_u25.png`/`lantern_u0.png` the void's face is at (430, 910) = (50%, 49%). A centred ~110 css px stack under a 0.72 scrim sits on the one lit thing in a night world for the hold and on the void for the settle.
FIX SOUND: yes — the shared CSS, one change for five worlds. At u100 the 21%-32% band (392-600 px) is sky (392-480) and the bamboo rim (480-600), the finial at (430, 625) just under it: the name over the horizon, the tower under it — as claimed.
CORRECTION: the u0 description is wrong. At u0 the 392-600 px band is NOT "the canal and the boat" — in `lantern_u0.png` the canal ends at y ≈ 250-400 (diagonal, left side); the band is tan ground, the RIGHT STALL's roof (x 460-860 from y 340 down) and the bubble band (425-485). Still clear of the void at 910, which is what the change buys. The GATE as written is fragile: "the largest cluster of amber pixels (R > 220, G > 170, B < 150)" — the tower's windows are six separate rows, and the market's paper lanterns and the stair's string lights are the same amber; assert instead that `rect('#titlecard .name').bottom` (css) is above the finial disc (the orange disc at the tower's apex: the largest component with R > 230, G in 150-200, B < 120 in the canvas twin) — one shape, one world.

### The HUD is live over the establishing shot
REAL: yes
WHAT I FOUND: `index.html:1688-1692` `<div id="timer">3:00</div> <div id="coins">✦ 0</div> ... <div id="growth">`, `:993` `#btnQuit { position: fixed; top: calc(56px + ...)`; `grep body.intro` — no hits in `src/` or `index.html`; the intro tick 9459-9494 touches no DOM; `matchClock -= dtw * clockSpeed` (8540) runs from frame one. In `lantern_u100.png` I saw "3:00" at (430, 70), "✦ 0" at (790, 55), ⌂ at (790, 155), "VOIDLING 1m … NEXT MUNCHKIN" across y 1720-1840 over the stair and stalls; the sheet's clock reads 3:00 → 2:56; "1m" at u100, "2m" at u75 — seen; `gm = \`${Math.round(r * 1.6)}m\`` at 4483 as quoted.
FIX SOUND: yes, with the sibling refuters' correction: toggle `body.intro` ON in `beginMatch()` beside `introT = COPY.introLen` (5585), not only in the tick, or the literal first painted frame still carries the HUD; clear it in the tick at 9473 and in `endMatch()` (4850).
CORRECTION: the "r 0.9 → 1.0 rounding, as the Game Day chair traced" is not traced — the Game Day refutation found no law that lifts an unfed void from START_R 0.9 to 1.0 and left it open. Repeat it as open, not as traced.

### A canal spirit speaks at 2.06 s, with the controls damped
REAL: yes (minor)
WHAT I FOUND: `bubbles.ts:214-217` the mute checks `#banner` only, then the rival slots — as quoted; `life.ts:2237-2238` `canal: [... 'float one for luck', ...]`, `:2235` `'somebody is eating a LOT tonight'` in `stalls`; `life.ts:5553-5556` `chatCd -= dt; if (chatCd <= 0) { chatCd = rand(1.8 - 1.0 * tense, 3.0 - 1.6 * tense); const near = peds.filter(... < 68)` — no intro test; `prototype3d.ts:8652` the damp — as quoted. `lantern_u50.png` (2.06): "Float one for luck" at (20-260, 425-485) on a frame with no void; u25/u0: the same line at (600-845, 425-485); settled: "Somebody is eating a LOT tonight" at (15-440, 425-485) beside "✦ 0".
FIX SOUND: yes — one line in the `kind !== 'rival'` block, keyed on `body.intro`.
CORRECTION: the gate should count `.vb.show:not(.rival)` — a slot is visible by `slot.el.classList.add('show')` (bubbles.ts:294) and hidden by removing it (261, 312, 393); the two `.vb` elements always exist in the DOM (bubbles.ts:179-184), so "count `.vb` elements" without `.show` reads 2 forever.

### The middle of the swing has no subject, and on Lantern no curve fixes it
REAL: yes (minor) — but the numbers describe a camera that does not exist for the first half-second; see WHAT THE TEAM MISSED.
WHAT I FOUND: `9511-9512` `const q = Math.max(0, Math.min(1, (u - 0.25) / 0.5)); const e = q * q * (3 - 2 * q);`, `9493` `camDist = 38 + 262 * k2 * k2;`, `1430-1433` the corridor comment, `lantern.ts:206-211` "walking due north crosses THE GREAT GATE, then LANTERN ROW ... That is the establishing shot and the match plan in one straight line" — as quoted. Hero (14, −175), spawn (31, 207.5), 383 units — arithmetic checks. My projection: hero centre leaves at 1.22 s, void enters at 2.52 s, gap 1.30 s (36%); `(u-0.4)/0.5` → 0.74 / 1.94 / 1.20; `(u-0.45)/0.45` → 0.71 / 1.77 / 1.06; look point pulled south to world y 3300 / 3700 / 4100 → bathhouse NDC x 0.83 / 1.19 / 1.54 at 0.39 s with the void's entry pinned at 2.50-2.51 — every number the reviewer gave reproduces to ±0.02. `lantern_u50.png`/`_canvas`: canal, boat, stalls, no tower, no void — as claimed. On this camera diagonal north is up-and-right, so the tower exits top-right — `lantern_u75.png` shows exactly that.
FIX SOUND: (a) yes. (b) NO as filed. `(u-0.45)/0.45` starts the slide at 0.71 s — and the camera does not finish ARRIVING on the bathhouse until ~0.6 s (the swoop below): there would be no hold at all, only a rush in and a slide out. (b) is only sound once the swoop is removed, and then it must be shot and judged at u100 AND u75, as the Game Day refuter said of the same tune.
CORRECTION: the gate's `heroOnScreen` — `heroBox` is a closure-local in `createBubbles` (bubbles.ts:200), not exported; exposing it is a small accessor. "Do NOT shorten introLen" is right for the fractions but the reviewer's comment rewrite at 1431-1433 should also say the first ~0.5 s are a swoop, because today they are.

### The canal, the only thing in frame at u50, is painted with discs
REAL: yes (polish — the discs are the authored look, mis-scaled for this one camera)
WHAT I FOUND: the reviewer's two candidate lines are wrong and "not traced to a line" is honest. The pale ellipses are traceable: `island.ts:1726-1750` — "7a. THE LANTERN STRINGS over the canal. Two hundred pools down the channel", `for (let d = 0; d < L; d += 260)` ... `for (const off of [-115, 115])` ... `pool(cx + nx * off, cy + ny * off, 180 + rnd() * 60, ...)` under `g.globalCompositeOperation = 'lighter'` (1724) — two lanes of light pools 180-240 map units (9-12 3D units) in radius, either side of the centre line, every 260 along the run, painted onto the ground bake with the comment "the reflection is what sells a canal at night". `lantern_u50.png`: eight to ten pale ellipses in two rough rows on the blue water — that is this loop, seen from camDist 86, where a 10-unit disc is a few hundred px. `island.ts:2116` is the ground-albedo comment and has nothing to do with the discs; `bakeContactShadows()` (2766-2795) harvests `userData.cshadow` discs (`assets3d.ts:85`) from edibles into one instanced mesh — that is the candidate for the DARK discs (16 boats at scale 2.2 and 150 float lanterns, all `afloat`, `island.ts:5668-5678`; and the torii at the left edge of u25), which I also cannot prove from the pixels.
FIX SOUND: yes as an experiment, with a correction: a keep-out of the pool bake on the canal deletes the authored "reflection" the level comment says sells the canal; the honest experiment is three frames at u50 — as shipped, pools off on the canal, pools at half core alpha and 1.5× radius on the canal (a tune inside `pool()`'s arguments at 1744-1746, no seeded draw: `rnd` there is its own LCG at 1730). Whichever reads as water under lanterns is the change.
CORRECTION: AT is `island.ts:1726-1750` (pale) and `prototype3d.ts:2766-2795` + `assets3d.ts:85` (dark, unproven). Not 2116, not 8534.

### The sky is a quarter of frame one and its moon is a pale smudge
REAL: no — the smudges are not the moons, the moons cannot be in the sky band from this camera, and the fix and gate as filed can neither remove the smudges nor produce a red moon.
WHAT I FOUND: `island.ts:882-889` paints `{ d: 640, el: -56, ..., size: 98, hue: '#c9563f' }` and `{ d: 880, el: -76, ..., size: 58, hue: '#c9a6ff' }` as quoted; `:977-979` places each sprite at `(cos(e)cos(az)·d, sin(e)·d, cos(e)sin(az)·d)` with `e = bd.el · π/180`, and `:3661` re-centres them on the camera every frame: `sb.o.position.copy(cam.position).addScaledVector(sb.dir, sb.d)` — they are LOCKED IN DIRECTION from the camera. `AZ = 3.927` (865) is the camera's own look azimuth. So: (1) they cannot move on screen between two frames of the same orientation, and the reviewer's own note says the smudges moved; (2) el −56° / −76° against a camera pitched 46.4° down with a ±16° lens puts them 9.6° and 29.6° BELOW the optical axis — the lower half of the frame, behind the ground: from the u100 camera (height ~177) the ray to the first body meets y=0 about 119 units out, at (41, −129), in the market — occluded. The sky band at the top of frame one is the direction 30°-38.5° below horizontal, past the valley rim; the bodies are at 56° and 76°. They are not in any intro frame, on this or any world (the Game Day refuter's "pale disc at (21%, 10%)" is the same mistake in the other direction). (3) Size: `sp.scale = bd.size · DISC_FIT` with `DISC_FIT = 0.40/0.29` (906) → 135 units at 640 → ~12°, ~700 px; the companion ~300 px. The smudges I measured in `lantern_u100.png` with pngjs are 47×60 px at (59, 376) and 33×31 px at (646, 493), mean rgb (137,135,164) and (122,115,163) — LAVENDER-GREY, saturation ~0.18, not white and not `#c9563f`; the canvas twin 0.25 s later has no component brighter than luminance 60 anywhere in its top 30% except the rim's lanterns and figures. What they are I could not settle from code: the sky canvas's lavender nebula clouds (`island.ts:532` `'rgba(186,166,255,ALPHA)'`) and the star field's larger twinkling points (675) are the candidates, and both move with the orientation change I found below.
FIX SOUND: no. `toneMapped: false` or a deeper body colour cannot touch a lavender smudge that is not the sprite, and cannot bring a body at el −56° into a sky band that ends at −38.5°. The GATE ("largest bright component in the top 25% has hue within 30° of #c9563f") fails before and after the proposed fix. If LIGHT wants a red lantern-moon over the bathhouse, the change is the body's ELEVATION (an `el` of about −33° to −36° for the intro camera, which then also moves it in every play frame that shows sky) — an owner-facing look change, not a polish item for this chair, and one to shoot before proposing.

## WHAT THE TEAM MISSED
- **THE FIRST HALF-SECOND IS A SWOOP, NOT A HOLD — and u100 is not the first frame.** The match reset seeds the camera on the VOID: `prototype3d.ts:6710` `camDist = 50;`, `6723-6726` `camOffset.set(0.62, 0.92, 0.62).normalize(); camFollow.set(voidState.x + camOffset.x * camDist, camOffset.y * camDist, voidState.z + camOffset.z * camDist);` — deliberately ("Spawn and the opening hand are hand-authored and identical every load ... has to include the camera", 6720-6722, which also records the measurement: "pitch 11.88 on match 1"). The intro tick's first frame then sets `camDist = 300` (9493, k2 = 1) and the look point to the hero (9513-9514, e = 1), and `camFollow.lerp(tmpV, 1 - Math.exp(-5.0 * dt))` (9564) drags the render position toward the orbit over the bathhouse — 317 units away — with a 0.2 s time constant: residual 37% at 0.2 s, 14% (~44 units) at 0.39 s, 4% at 0.63 s, 1% at 1.0 s. `camera.lookAt` (9566) is unsmoothed, so frame 1 is a ~12° pitch look from over the gate toward the bathhouse 383 units north — the code's own "corridor", by accident, for a quarter of a second — then a rush up to the orbit. THE PIXELS AGREE: between `lantern_u100.png` (0.39) and `lantern_u100_canvas.png` (0.63) the finial moved ~15 px (625 → ~608) while the bamboo rim moved ~200 px (≈480 → ≈280-330). A pure dolly 246 → 216 moves a point 350 px off-centre by at most ~50 px; a 200 px shift is the optical axis still rotating. So the "hold" is ~0.4 s of swoop plus ~0.8 s of hold, the Lantern hand-over is the longest of the five AND its swoop is the longest, and the shot a child sees at t = 0 is unphotographed (JSON: tWanted 0.06, tPage 0.39). Path: if the hold is wanted, seed `camFollow` at the intro's own frame-1 target in the reset when `COPY.hero` — `camFollow.set(hero.x + o.x·300, 0.45 + o.y·300, hero.z + o.z·300)` — one line, no seeded draw, identical every load; if the swoop is the corridor the code promised, say so at 1431-1433 and keep it. Decide by shooting t = 0.0, 0.1, 0.2, 0.3: `qa/firstframe.mjs` should hook the first rAF after the card click (`waitT(0)`) and shoot before the next frame, and the gate should assert `camDist` from `__matchState` ≥ 290 AND the rendered camera's distance to the look point within 5% of it by t ≤ 0.1 (needs `camera.position` on the debug surface — three numbers).
- **The moons are unseeable from the intro camera on every world** (finding 7 above) — and the same lavender smudge appears in Game Day's u100. One hide-and-shoot of the sky canvas, the star field and the two sprites would name it for both packs.
- **The magnet well is live on Lantern too.** `9377` `const inWell = d < reach && e.radius < 2.5 && ...` has no `controlsLive`; "nothing eaten, nothing to apologise for" is the spawn's geometry (the nearest small meals are the 150 float lanterns at ±5.5 units of a canal centre line 18 units west of the spawn; `reach = R·2 + e.radius·2.4` ≈ 3 units), not a mechanism. The Maple refuter's `controlsLive &&` should land on five worlds regardless, and Lantern's `matchEaten` at settled is the gate here as there.
- **`body.intro` must be set in `beginMatch()`** (5585), not only in the tick — or the literal first painted frame, which is this stream's subject, still carries the HUD. Both sibling refuters said it; the Lantern report dropped it.
- **The shadow map flips on in one frame at introT ≤ 0** (9469-9472). At night with a low sun this may be invisible; two canvas frames at introLen ± 0.05 settle it for the cost of nothing.
- **The reshoot should include the owner's viewport** (`--views=440x956@3`): the 21vh band, the finial's y and the bubble band all shift a few px.

SURVIVED: 6 of 7.
