# REFUTATION — GAME DAY establishing shot (Stream D, skeptic to the cinematographer)

Read the same ten images and every cited line myself. Also read what the cinematographer did not: the magnet loop (`prototype3d.ts:9366-9420`), `capture()` (5121-5151), the growth law (8748-8778), the match reset (6699), the Game Day `plant`/`drop` pair (`island.ts:5841-5884`), `spawn3()`/`nearSpawn()` (island.ts:229-248), the sky tables (`island.ts:610-622`, 866-870), `qa/firstframe.mjs` (whole), the Maple refutation (`refute-swing-maple.md`), and `maple_u50.png` as the control for the card. I projected the hero and the void through the code's own camera (camOffset (0.62,0.92,0.62), 32° vertical FOV, 430/932, camDist 38+262k², the 0.25/0.5 smoothstep) with a 40-line node script in the scratchpad — arithmetic, no browser.

## VERDICT ON THE VERDICT
NO-SHIP is right: three majors are in the page frames themselves — a live HUD over the aerial at the first frame (`gameday_u100.png`), a trash barrel pulled into the void before the hands exist (`gameday_u25.png` → `gameday_u0.png`), and a title card whose centre is, by CSS, the exact pixels the stadium holds and the void lands on — and the card's wall-clock animation is why no probe in this pack could see it at all. The sky finding is not a defect and should not have been filed.

## PER FINDING

### The card and the shot are on two clocks — and the card was in none of these frames
REAL: yes
WHAT I FOUND: `index.html:548` `#titlecard.show { animation: cardFade 4.2s ease forwards; }` and `:557` the keyframes, as quoted. `prototype3d.ts:5582-5585` `tcEl.classList.remove('show'); void tcEl.offsetWidth; tcEl.classList.add('show'); titleUntil = tClock + 4.6; ... introT = COPY.introLen;` and `:8527` `const dt = Math.min(0.05, dtRaw);`, `:8533` `tClock += dt;`, `:9460` `introT -= dt;` — as quoted. `:1408-1411` "3.4 still finishes inside the 4.2s title card" — as quoted. In all six Game Day page frames I saw no card; on the same instrument `maple_u50.png` (tPage 1.10) shows "LEVEL 1 / MAPLE FALLS / the little void is hungry · eat the town" at full opacity. So the card was armed (the code path is the same `beginMatch`), and on Game Day it had finished — 4.2 wall-seconds — before the first sample at 0.35 match-s, i.e. the first seven-plus frames took over 4.2 s of wall time. The draw-call comment at 9462-9464 (4,694 calls, 1.40M triangles on the opening frame) is the reason Game Day differs from Maple. The coupling is real: below 20 fps the match clock runs slower than wall time and the card leaves before the dive lands.
FIX SOUND: yes. A paused WAAPI animation with `currentTime` driven from the tick beside `tClock += dt` is one clock; `titleUntil` is already on that clock. Delete the CSS animation so the two cannot double-run. No seeded draw, no budget, no art.
CORRECTION: severity on the owner's phone is unproven — nobody has a Game Day intro frame-rate number, and the reviewer says so. What IS proven is that the card is ungateable today: any probe that reads it at a match-time stamp reads noise (the Maple refutation found the same ~0.8 s drift). Keep it major for that reason, and run the reviewer's `dtRaw` experiment before claiming a child ever sees the drift. Also note `qCd = 6` at 5553 ("Six seconds of grace covers the camera swoop… the frames that used to demote a good phone before play began") — the code already expects the swoop's frames to be slow enough to trip the quality ladder, which is evidence in the reviewer's favour.

### The card is centred on the subject
REAL: yes (by code; invisible in this pack for the reason above)
WHAT I FOUND: `index.html:543-547` `align-items: center; justify-content: center; ... radial-gradient(ellipse 78% 34% at 50% 50%, rgba(9,5,20,0.72) 0%, rgba(9,5,20,0.55) 45%, rgba(9,5,20,0) 78%)`; `:551` `.name { font-size: clamp(34px, 12vw, 62px) ...}`; `prototype3d.ts:1392` the tagline — all as quoted. In `gameday_u100_canvas.png` the field's yellow centre disc is at (51%, 50%) and the bowl spans y 34%-62%; in `gameday_u25.png` and `gameday_u0.png` the void's face is at (50%, 49%). A centred 110 px stack with a 0.72 scrim lands on the field for the hold and on the void for the settle.
FIX SOUND: yes — the same CSS as Maple's, one change for five worlds. In the page frame `gameday_u100.png` the treeline is y 12%-22% and the bowl's north stand begins at ~36%, so a 21%-32% band sits on the north concourse and the lot's top edge, above the bowl; at u0 it is the red pickup's bed and the lane stripe (`gameday_u0.png` y 21%-32%), clear of the void. The Maple refutation's placement checks (`#news` top on a 375x667 phone, `#evolve` suppressed by `titleUntil`) apply here unchanged.
CORRECTION: none on substance. The proposed gate's green test ("G > 120, G > R+40, G > B+40") will also catch the treeline's pines and the verge at u100 — take the largest connected region INSIDE the bowl's red ring, or simply assert the `.name` rect's bottom is above the field disc's centroid (y 50%), which is what the change actually buys.

### The HUD is live over the establishing shot
REAL: yes
WHAT I FOUND: `index.html:1688-1696` the four elements, `:117-124` `#timer { position: fixed; top: max(12px, ...) }`, `:157-166` `#growth { position: fixed; ... bottom: calc(14px + env(safe-area-inset-bottom)) ... width: min(92vw, 460px) }`, `:441` `#coins { position: fixed; top: calc(12px + ...) }`, `:993` `#btnQuit { position: fixed; top: calc(56px + ...) }`; `grep body.intro` — no hits in src or index.html; the intro tick 9459-9494 touches no DOM. In `gameday_u100.png` I saw "3:00" at (50%, 4%) over the sky, "✦ 0" at (92%, 3%) and ⌂ at (92%, 8%) over the sky and trees, and "VOIDLING 1m … NEXT MUNCHKIN" across y 92%-98% over the lot's south stands and two walking fans; `gameday_u100_canvas.png` is the same moment clean. The clock reads 3:00, 2:59, 2:59, 2:58, 2:57, 2:56 across the sheet. "1m" at u100 and "2m" at u75 with nothing eaten — seen; `gm = Math.round(r * 1.6)` at 4483 as quoted.
FIX SOUND: yes, with the Maple refutation's correction carried over: toggle `body.intro` ON in `beginMatch()` beside `introT = COPY.introLen` (5585), not only in the tick, or the very first painted frame — the one this stream is about — still shows the HUD; clear it in the tick at 9473 and in `endMatch()`.
CORRECTION: the "1m → 2m" mechanism is asserted from a comment, not the code. `formProgress`'s comment (4447-4449) says "spawn r=1.0 — START_R 0.9 is the SHRINK floor", but the match reset at 6699 is `voidling.setRadius(START_R)` = 0.9, and I found no law that lifts an unfed void to 1.0 (`scoreFloor` at score 0 is `START_R * (1 + 0)` = 0.9, `lawCap` is a ceiling). Something does move the label to "2m" (r ≥ 0.9375) by t≈1.1 on both Maple and Game Day with no bite visible, and I could not name it from source. The fade hides the flip either way; whoever lands the fix should print `r` at t=0 and t=1.1 and settle whether spawn is 0.9 or 1.0, because the growth bar's "begins EMPTY" promise and the eat gate below both depend on the answer.

### Crowd speech lands during the cinematic, before the controls exist
REAL: yes (minor)
WHAT I FOUND: `bubbles.ts:214-217` — the mute checks `#banner` only; `HUD_BANDS = ['banner', 'evolve', 'news', 'titlecard']` (96) is the dodge list. `life.ts:4472-4478` the grill vignette at (5950, 8300) with 'Somebody find me the long tongs.' — 510 map units, 25.5 3D units, due north of `GD_SPAWN` (5950, 8810). `newsroom_gameday.ts:821` 'My father brought me here first.' in the fan pool. In `gameday_u50.png` (tPage 2.01, `introT` still 1.4) the tongs line sits at (2%-57%, 76%-80%) with `velX *= 0.9^(dt·60)` (8652) still damping; in `gameday_u25.png` it is at (43%-98%, 23%-26%); in u0 and settled the fan line occupies the same band.
FIX SOUND: yes — `if (document.body.classList.contains('intro')) return;` in the `kind !== 'rival'` block is the same shape as the banner rule and keys on the match clock. The Maple refutation's gate note applies: ambient bubbles carry no class, so count active slots, not `.rival`-less elements.
CORRECTION: none.

### The void eats a cup before the child touches the screen
REAL: yes — the eat is real; the object, the determinism claim, the fix and the gate are all wrong.
WHAT I FOUND: In `gameday_u25.png` I saw, at the void's 3 o'clock (60%, 51%), a crimson cylinder with a dark ring and a grey domed top carrying red-and-white cups, lifted and tilted toward the void — identical to the barrel standing at (44%, 70%) in the same frame. That is `makeTrashBarrel()` (`tailgate.ts:835-846`: `CylinderGeometry(0.46, 0.38, 1.1)` in `pick([CRIM, ASPHALT, GRASS])`, a `CHAR` torus and lid, a `0x9aa0a8` sphere, `cupParts(...)` on top), not a cup. In `gameday_u0.png` it is gone. The growth fill is absent at u50 and a dot at u25 (8%, 97%), wider at u0 — as claimed. The mechanism is the magnet, exactly as the Maple refutation found: `prototype3d.ts:9376-9377` `const reach = R * 2.0 + e.radius * 2.4; const inWell = d < reach && e.radius < 2.5 && ...` with no `controlsLive`/`introT` condition, and 9395-9396 `if (d < R + e.radius * 0.7) capture(e);`. The barrel is planted at r 0.8 (`island.ts:5966` `plant('lot', 70, 30, 0.8, TG.makeTrashBarrel)`), so at R 0.9 its well is 1.8 + 1.92 = 3.72 units and it clears the eat gate (0.8 ≤ 0.9·1.11). The spawn note's "170 clear of the nearest row centreline" (gameday.ts:290) is 8.5 units from the TRUCK rows; small props have no spawn clearance at all — `nearSpawn()`/`SPAWN_KEEP_OUT` (island.ts:239-248) is called from exactly one place, `life.ts:2579`, for people; the Game Day `drop()` (island.ts:5841-5848) tests `GD.spotOpen` claims only.
FIX SOUND: no. "Nudge `GD_SPAWN` by ±20" and "a keep-out in the drop that places it" are both aimed at one prop under one seed. The smallest change that closes it on every world and every load is the Maple refutation's: `const inWell = controlsLive && d < reach && ...` at 9377 (and `capture()` is already unreachable without the well or a moving void). If PLAY also wants clean ground at the Game Day spawn for the picture's sake, `if (nearSpawn(x3, z3, SPAWN_KEEP_OUT)) return;` in the Game Day `drop()` after `P3(p2)` — a reject, which consumes no extra draw (the scatter has already sampled) and is world-local.
CORRECTION: (1) "determinism says every load" is false for this world. The Game Day lot is scattered with `Math.random` (`island.ts:5877` `GD.scatterInRegion(REG(id), n, Math.random, clear, { sep })`, `:5883`, `:5939` `GD.lotSlots(Math.random)`), and I found no seeding of `Math.random` anywhere in `src/` or `index.html` — the probe seeds it itself (`firstframe.mjs:104`). So in the shipped build the barrel is somewhere else on every load and the eat is a probability, not a certainty; the well is what makes the probability high (a 3.7-unit circle in a lot planted with 70 barrels, 220 chairs, 130 cooler stacks and 70 footballs). (2) The gate "count of edibles with `eaten`, assert 0 at settled" passes on today's build: `e.eaten = false` at 9367 when the spiral ends (`scene.remove(e.mesh); e.eaten = false;`). Expose `matchEaten` (3355, "props eaten THIS match") on `__matchState` and assert it is 0 at settled; `R <= 1.0` is only a valid second assertion once the spawn-radius question above is settled.

### The middle of the swing has no subject
REAL: yes (minor; a tune with an experiment)
WHAT I FOUND: 9505-9513 `const q = Math.max(0, Math.min(1, (u - 0.25) / 0.5)); const e = q * q * (3 - 2 * q);` and 9493 `camDist = 38 + 262 * k2 * k2;` — as quoted. `gameday_u50.png`/`_canvas` (2.01/2.21): the lot only — no bowl, no void — as claimed; the sheet reads stadium, stadium-leaving, LOT, void, void, void.
FIX SOUND: yes as an experiment — one constant, no seeded draw, judged by a reshoot. But temper the expectation (below).
CORRECTION: the numbers. Projecting the hero POINT and the void through the code's camera: today the hero centre leaves the frame at k≈0.65 (t≈1.2 s) and the void enters at k≈0.30 (t≈2.38 s); the bowl's rim (rx 76 units on plan) lingers to about k≈0.57, which is what `gameday_u75_canvas.png` (k 0.58) shows — the west stand cut by the right edge. So the subject-less window is ~0.9-1.2 s, not 0.8. With the handover at 0.4 the void enters at k≈0.46 (t≈1.84 s), not k≈0.52/1.6 s, and the hero centre leaves at k≈0.77 (t≈0.78 s): the gap shrinks by ~0.3-0.4 s, and at u75 (t 0.85) the bowl is already sliding out at the right edge — the second frame gets worse to buy a shorter third. The reviewer's own revert rule ("if the bowl at u100 now reads as already sliding, revert") should be read at u75, where the cost lands. The gate's `heroBox` is a closure-local in `createBubbles` (bubbles.ts:200), not exported; exposing it is a small accessor, not a read.

### The sky says night over an afternoon-lit field
REAL: no — a deliberate, measured design shared by all five worlds.
WHAT I FOUND: in `gameday_u100_canvas.png` I saw a navy-magenta sky with star points and a pale disc at (21%, 10%) — as described. The code says this is the authored sky: `island.ts:611-622` `SKY_MOOD.gameday: { tint: '#7a2b52', tintA: 0.70, fog: 0x241120, bgI: 0.34 }   // magenta dusk over the lot`, with the comment "GAME DAY WAS THE ONE WORLD NOBODY PHOTOGRAPHED, and it was the worst of the five: flat bright magenta at the coast with not one star in it … The starfield is ADDITIVE, so a sky that never reaches black cannot show a star" — the darkness was tuned IN so the stars read. `island.ts:866-870` places "a hot magenta dusk giant over the parking lot, and a pale companion" (`hue: '#ffd9a0', size: 49`) — the disc I saw is the companion. Every world's sky is a cosmos with two bodies (SKIES has a row per world); the diorama-in-space premise is the game's, and it is the subject of the owner-side task "Space does not look like space", which pulls the other way. The light rig's "late afternoon" is the ground's key; the sky is not a weather report.
FIX SOUND: no. The proposed gate (top 8% luminance bright "for a day rig") would fail all five authored skies, and a sky-only change to daylight removes the starfield the file was tuned to show.

## WHAT THE TEAM MISSED
- THE MAGNET, again. The reviewer had the Maple report in hand (it is in COVERAGE) and still proposed a spawn nudge; the Maple refutation had already found the well runs through the intro with no condition. One `controlsLive &&` closes it on five worlds; a keep-out closes it on one seed.
- GAME DAY'S LOT IS NOT "IDENTICAL EVERY LOAD". `plant`/`lotSlots` draw from unseeded `Math.random`; only the probe seeds it. The GOVERNOR "HANDS OFF" entry ("Spawn and the opening hand are hand-authored and identical every load") is true of the spawn POINT and of Maple's `mrnd` stream; on Game Day the props around the void in the first frame differ per load. For a stream whose whole subject is the first frame, that is worth a line — either it is fine (the lot is uniform enough that any load reads the same) or the owner's order is not being met here, and the reviewer did not say which.
- `e.eaten` RESETS. The gate as written passes today (9367). `matchEaten` is the durable count.
- SPAWN RADIUS: the comment says 1.0, the reset says 0.9, and the label says the void grew in its first second with nothing eaten. Nobody in this pack has printed `r` at t=0.
- THE SHADOW MAP RETURNS IN ONE FRAME. 9469-9471: `renderer.shadowMap.enabled = false; sun.castShadow = false` for the whole dive, restored the frame `introT <= 0` — on a world whose light rig sells itself on "a shadow two to three times its own length across flat tarmac" (697-708). The frame the controls go live is also the frame every long shadow appears. I could not confirm a visible pop from this pack (u25 at 3.12 and u0 at 3.79 straddle it, but the framing differs and the blob contact shadows are on throughout); it is a cheap thing to shoot — two canvas frames at introLen−0.05 and introLen+0.05 — and if it pops, fade `sun.intensity`'s shadow contribution over the last 0.3 s rather than flipping the map.
- The reshoot should include the owner's viewport (`--views=440x956@3`, already accepted by firstframe.mjs) — the 21vh band and the bubble band are a few pixels different there.

SURVIVED: 6 of 7.
