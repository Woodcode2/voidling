# VERDICT: SOUND WITH CORRECTIONS — the deletion stands on the owner's order; three of the commit's four "twins" and its airtime claim do not survive measurement

Commit 0efda23 removes five family cards (welcome, join, eaten, surge, stuffed), announceJoin() and ARCH_TAG, and adds comments only (diff re-read: `git show 0efda23 -- src/prototype3d.ts | grep -E '^[-+]'`). Nothing it deleted breaks anything at HEAD (tsc clean; no write-only state; every kept channel measured firing), the owner asked for the cards to go, and reverting would restore neither the information the cards carried (the world-space twins never carried it at the right moment) nor the crowd's voice (it is gagged elsewhere). So the landing is SOUND. The commit's ACCOUNT of why is not, and the governor must not carry it into the ledger:

1. THE GAG IS TOTAL, NOT 39%, AND THE REMOVAL RETURNED ZERO AIRTIME. `#banner.show` runs `bnr 2.4s … forwards` (index.html:398) and ends at opacity 0, but the class is only removed by the re-paint dance (prototype3d.ts:3157) and the match-boundary reset (:6471). bubbles.ts:216 and :359 test the class. Measured on two worlds: class on for 7790/7790 samples after the first paint (Maple, t=4.62) and 7946/7946 (Pirate, t=16.59); crowd bubbles shown after the first paint: 0 and 0 (2 and 8 before it). The town has been silent from the first card to the whistle of every match, whatever cards exist. The "39% duty / one impression every 5.6 s" the commit cites is an inherited comment (first appears in 589e31e, which added audio probes, not a banner probe; no measurement file in history). Visible-card duty at HEAD: 28.9% (24 paints, one per 7.5 s) and 26.9% (21 paints, one per 8.6 s).
2. THE JOIN TWIN IS OFF-SCREEN. 8/8 joins on two worlds put the halo outside the frame at the moment of joining (0 on-screen samples in the first second, every join); first sight +1.4 s to +136.3 s later. The archetype line the family speaks on joining (rivals.ts:1095, RIVAL_VOICE.arch) was audible (d<55, prototype3d.ts:2444) for 1 of 8 joins. A join at HEAD is one audio.alert().
3. THE SURGE TWIN SAYS THE OPPOSITE AT THE MOMENT THAT MATTERS. In both natural surges the halo was GREEN when onSurge fired (Pirate BIGSHOT r=3.67 vs player 4.56; Maple JELLY 0x54e88a → her own pink): the ring said "you can eat him" where the card said "grew BIGGER than you". Red arrived +0.88 s / +1.34 s; over the 23 s Pirate surge the halo was red 68.8% of frames, on-screen 4.4%, red AND on-screen 2.8%. "RED for the entire surge" is false.
4. THE STUFFED TWIN IS 41 SECONDS LATE. At onStuffed the hunter's halo was RED in 3/3 runs (ratio 1.54–1.56; on-screen in 1/3) and stayed red 463/463 samples for the next 11 s on Maple; the GOLD prize halo first appeared +41.3 s later on the natural Pirate run, when the player had outgrown her. The card's "now is your chance" and the halo's "now" are different moments. Her own bubble ("Too full… to chase… ugh", rivals.ts:1127) and the BUGLE ALERT (breakingNews) DO carry "too full" at the moment, when she is within 55 units.
5. THE EATEN TWIN IS REAL. bubbles.float() fires at the kill site with title + name + points on every rival kill measured (natural: "Baby ECHO DEVOURED! +188" t=48.86 Maple, ECHO on-screen; "Uncle BIGSHOT DEVOURED! +455" t=173.83 Pirate; forced: "Auntie NIBBLES DEVOURED! +13199"), photographed in cards-skeptic-kill.png. The one deletion the commit fully justified.

## What I ran
- `git show 0efda23 -- src/prototype3d.ts | grep -E '^[-+]'` — deletions: ARCH_TAG, announceJoin call+body, three announceHtml (eaten/surge/stuffed), one welcome cardHtml; additions: comments only.
- `npx tsc --noEmit -p tsconfig.json` exit 0. `npx tsc --noEmit --noUnusedLocals -p tsconfig.json` exit 2: 70 TS6133 repo-wide, 19 in prototype3d.ts, none an identifier the commit touched.
- `git log --all -S'one impression every 5.6'` → 589e31e, 9486028 only. `git show 589e31e --stat` → 486 qa/ files, added qa/_mix.mjs/_mix2.mjs (audio dBFS probes).
- cards-skeptic.mjs (scratchpad; playwright, 430×932, swiftshader, :4177, greedy-edible bot, per-frame samples on __matchState().t of #banner.show, every paint via innerHTML setter, every .vf float, every .vb bubble, every rival's real halo material hex + distance + on-screen through __cam; render disabled between photographed moments and restored two frames before each shot). Three lock holds: 14:59 maple (join shot only — probe assumed CHOMPZILLA was the hunter; NIBBLES was; run lost at the surge step), 15:10 maple late (8004 samples) + 15:12 pirate natural (8726 samples), 15:16 maple kill re-shoot with `.vf.go` slowed to 60 s for the photograph only (the 0.9 s vfRise is wall-clock; the match clock under swiftshader is ~20× slow). Headline prints: `BANNER DUTY: 176.0s of 180.4s = 97.6% paints=24` (maple), `BANNER DUTY: 164.0s of 180.4s = 90.9% paints=21` (pirate), `BUBBLES: 29 rival: 27` / `29 rival: 21`, per-rival JOIN/SURGE/STUFFED lines as recorded in the appendix.
- Shots (6): docs/crews/round-5/shots/cards-skeptic-{join,stuffed,surge-start,surge-red,kill,crown}.png.

## What I checked on disk
prototype3d.ts :2394-2411 (onJoin, audio.alert only), :2440-2445 (onSpeak d<55), :2447-2487 (onRivalEaten → float at kill site), :2612-2648 (onSurge ring, onStuffed breakingNews), :3128-3175 (bannerQ, paintBanner, BANNER_READ), :4240-4342 (rows/myRank → crown, mirror, "you passed", brag bubble), :4871-4935 (end screen), :6471 (reset), :9296-9314 (welcome site, 3000 ms setTimeout); bubbles.ts :170-174 (vfRise), :205-218 (say gag), :296-305 (float), :356-361 (shortener), :385 (opacity convention); rivals.ts :141-199 (RIVAL_VOICE.arch), :409/:1845 (`full` is the lane flag), :1034-1052 (surge window 0.55-0.72 matchLen, pick 40..200), :1080-1096 (join placement + arch line), :1100-1130 (stuffed), :1134-1140 (surge growth 0.55/s), :1630 (swallow rule), :2015-2031 (halo colours), :1675 (stale "banner promises"); index.html :361-362 (#banner opacity 0), :385-396 (.bIco/.bSub/.bMul live, .bDot dead), :398-405 (bnr); qa/ grep for card strings (only _rf_hunt.mjs:127 and _rfbeats.mjs:73, both underscore scratch, neither in gate.mjs; no probe asserts ARCH_TAG or crown strings; crownprobe.mjs never existed).

## Kill shots
None against the landing. Against the commit's claims, the five numbered facts above; the frames: cards-skeptic-join.png (no rival in frame at the join), cards-skeptic-stuffed.png (red ring on NIBBLES under "Too full… to chase… ugh"), cards-skeptic-surge-start.png / surge-red.png (JELLY outside the frame both times; only her edge-clamped bubble reaches the screen), cards-skeptic-kill.png (the floater twin, real), cards-skeptic-crown.png ("👑 YOU ARE IN FRONT! BIGSHOT is behind you" with the gold ring).

## Corrections (verbatim)
C1 — the gag (new bug, found by this refutation; Rule 2 satisfied by the measurement above: 0 crowd bubbles after the first paint on two worlds). src/proto3d/bubbles.ts:216, replace
`        if (ban && ban.classList.contains('show')) return;`
with
`        if (ban && ban.classList.contains('show') && Number(getComputedStyle(ban).opacity) > 0.06) return;`
and src/proto3d/bubbles.ts:359, replace
`        if (ban && ban.classList.contains('show')) {`
with
`        if (ban && ban.classList.contains('show') && Number(getComputedStyle(ban).opacity) > 0.06) {`
(the file's own convention at :385; #banner's base opacity is 0 and bnr ends at 0, so "a card is up" becomes true only while one is visible). Probe to land with it, qa/bannergag.mjs: one Maple match, greedy bot, MutationObserver counting `.vb.show` elements whose class lacks `rival` after the first #banner innerHTML set; FAIL if that count is 0 or if `getComputedStyle(#banner).opacity < 0.06` while a non-rival say() is still being discarded — before C1 it prints 0/0 (this run), after it must print > 0.
C2 — the three "No card:" comments the commit added must state what was measured, not what was assumed. Replace prototype3d.ts:2628-2630 with
`  // No card. The halo is NOT a twin at this moment: at onSurge the rival is smaller than the player (green ring) and turns red ~1 s later as it grows (0.55/s toward 1.26x); measured 2026-09-02 red-and-on-screen 2.8% of a 23 s surge. This ring in the rival's colour is the whole cue.`
Replace prototype3d.ts:2648-2649 with
`  // No card. At this moment her halo is RED (she is ~1.5x you); GOLD comes only once you outgrow her (measured +41 s, natural). "Too full" lands here through her own stuffed line (rivals.ts:1127, if within 55) and breakingNews below.`
Replace the sentence "and the banner's measured duty cycle is 39% — so a large share of the town's silence was the family announcing itself." (prototype3d.ts:2405-2406) with
`  // — and bubbles.ts tested #banner's `show` class, which is never removed after the first paint, so the town was silent from the first card to the whistle regardless (see the fix in bubbles.ts).`
C3 — index.html:396: delete the `#banner .bDot { … }` rule (no producer since announceJoin went).
C4 — stale comments: prototype3d.ts:2452 "no breakingNews here: announceFam already puts a full-screen card up for this" → "no breakingNews here: the float at the kill site is the card"; :2473 drop "a full-screen card," from the list; :3062-3071 delete the orphaned announceJoin docblock (the function is gone; the block now heads announceBeat); :9303 "Two banner cards riding the existing queue" → "One banner card, three seconds after controls go live (setTimeout 3000, wall clock — the delay once spaced it behind the welcome card and is kept as pacing)"; :2409-2411 "while the banner and the alert that actually tell the player were on it" → "while the alert that actually tells the player was on it"; src/proto3d/rivals.ts:1675 "the one character the banner promises \"CHASES you\"" → "the one character whose join line promises \"I am CHASING you\"".
C5 — for the ledger: the "39% duty cycle" comment at prototype3d.ts:3128 and :4265 is unmeasured and should read "(visible-card duty measured 26.9–28.9% at 0efda23, 2026-09-02; the class-based gag was 100%)".

# Appendix — incremental record, in the order it was measured
<!-- incremental notes, 2026-09-02 ~09:50 UTC -->
## What I checked on disk (running notes)
- `git show 0efda23`: one file, src/prototype3d.ts, +28/-25. Deletions: ARCH_TAG (:2394), announceJoin call (:2404) and body (:3065-3071), the eaten card (:2462-2464), the surge card (:2626), the stuffed card (:2644), the welcome card (:9308). Nothing else. Crown cards at :4281/:4299/:4314 untouched — confirmed at HEAD.
- The 39% figure: `git log -S"39% duty"` → first appears in 589e31e (2026-08-16, "Take the unlicensed theme.mp3 out of the shipping bundle" — a commit that also dumped the whole qa/ tree). Three later comments (:2405, :4265, and the new one in onJoin) all cite it. No probe file in qa/ contains "39%" or "5.6" for the banner (grep). The figure is INHERITED, not re-measured for this commit.
- bubbles.ts:214-218 (say): `if (kind !== 'rival') { if banner.show → return; if a rival bubble active → return }`. So the gag is real for crowd/'event'/'panic' kinds; 'rival' kind is NOT gagged. float() (:296) has no banner check at all. bubbles.ts:353-361 additionally shortens live crowd bubbles to 0.6s when a banner is up.
- rivals.ts:1042-1046: a surge is picked ONLY from rivals at distance 40..200 from the player. rivals.ts:2028: halo red only when rv.r > pr*1.11; surge grows r toward surgeR at rate 0.55/s ("grow, don't pop", :1139) — so red is not instant.
- rivals.ts:2022-2024: GOLD pulsing halo requires `isHunter && !hunting && rv.r > START_R*2 && pr > rv.r*1.05` — i.e. it is the PRIZE colour (player already bigger), not a "stuffed" colour. onStuffed (:1125-1127) fires when hunting ends regardless of size.
- prototype3d.ts onJoin comment (kept by the commit): the join happens "at the rival's own turf, up to 165 units off and usually nowhere near the screen".
- `npx tsc --noEmit -p tsconfig.json` at HEAD: exit 0 (ran 2026-09-02 ~10:55 UTC).
- Card-string assertions in probes: `qa/_rf_hunt.mjs:127` looks for a BANNER matching /too full/ (falls back to the STUFFED sample if absent — `cardAt: card ? card.t : stuffed.t`), `qa/_rfbeats.mjs:73` looks for /is too full/. Both are underscore scratch probes, neither is in qa/gate.mjs (grep). No probe in qa/ or scripts/ matches "CHASES", "copies your route", "runs from everything", "only eats big", "slow and steady" (the ARCH_TAG strings) — the only "slow and steady" hit is a GRUMPS voice line in rivals.ts:199. No probe asserts on "YOU ARE IN FRONT" / "TOOK THE LEAD" / "you passed" either; the `crownprobe.mjs` the crown comment cites (:4267) is in no commit of this repo (`git log --all -- qa/crownprobe.mjs` is empty).
- `#banner .bDot` CSS (index.html:396) — its only producer was announceJoin. Now dead CSS.
- Stale comments left by the commit, all in src/prototype3d.ts: :2452 "announceFam already puts a full-screen card up for this" (no such function; the card is gone); :2473 "a full-screen card" in the ring-count list; :3062-3066 the announceJoin docblock ("A rival has arrived. Was: pink 30px text…") now sits directly above announceBeat with no function of its own; :9303 "Two banner cards riding the existing queue" (one remains); :2408-2411 "while the banner and the alert that actually tell the player were on it" (the banner no longer exists on a join).
- The crew's own bannermix log (docs/crews/round-4/hud-subtract.proposal.md:139-150, parent build): 4 banner cards in match t=0..70, 3 of them family. 4 × 2.4s / 70s = 13.7% — on THAT run the whole banner was under the 39% the commit cites.
- bubbles.ts:11 kinds are 'ambient' | 'panic' | 'event' | 'rival'. The gag at :214 covers the first three. 'event' is the HERO's own set-piece bark (prototype3d.ts:5311, positioned at voidState) — not family speech. So the commit's "crowd and event bubble" wording is right; family ('rival') bubbles were never gagged by the banner.
- Analytic (source, to be confirmed by probe): the SURGE halo. rivals.ts:1049 pins surgeR = max(rv.r, pr*1.26); :1139 eases r toward it at 0.55/s; :2028 paints red only once rv.r > pr*1.11. Starting from a rival at 0.80x the player (the softCap floor, :1088), the fraction of the gap still to close when red begins is (1.26-1.11)/(1.26-0.80)=0.326, and exp(-0.55t)=0.326 gives t≈2.0 match-seconds of a NON-red halo at the top of every surge, before the ring ever says anything. And the pick (:1042-1046) is restricted to 40..200 units from the player.
- Analytic (source): the STUFFED halo. GOLD (:2022-2024) needs `pr > rv.r*1.05`. onStuffed (:1125) fires the frame the hunt ends, when she "looms at ~1.5x whatever you are" (:1108-1111). So at the moment the deleted card said "too full — now is your chance", the halo is RED (rv.r > pr*1.11), and it turns gold only after the player has outgrown her. The card's "now" and the halo's "now" are different moments.
<!-- resumed 2026-09-02 ~11:40 UTC by a second skeptic after the first died at the session limit -->
- Re-read the diff myself (`git show 0efda23 -- src/prototype3d.ts | grep -E '^[-+]'`): the only lines removed are the ARCH_TAG table, the announceJoin call and body, the three announceHtml calls (eaten/surge/stuffed) and the welcome cardHtml call; the lines added are comments only. No logic was added. The commit message's account of the diff is accurate.
- Write-only sweep: `npx tsc --noEmit --noUnusedLocals -p tsconfig.json` at HEAD (exit 2, 70 TS6133 hits repo-wide, 19 in prototype3d.ts: makeVoidBody, applySkinToBody, HAT_MAX_W, applyHatLod, buildHat, VOID, STICKERS, WorldKey, census, lidOpen, m×2, mealBySize, pickHeadline, fillHeadline, R@4241, startT, skinGrad, stallNudged). NONE is an identifier the commit touched — every deleted card's inputs (FAMILY_TITLE :2484, esc, cardHtml, announceHtml, rivalEv.marquee) still have a reader. Nothing became write-only because of 0efda23. Kill angle 4: no kill.
- CSS at HEAD (index.html:385-396): .bIco/.bSub/.bMul still have producers in src (bMul ×1 via announceBeat, bSub ×3 via the crown cards); `.bDot` (:396) has none — its only producer was announceJoin. Dead rule, cosmetic.
- Debug surface available for the browser probe (prototype3d.ts:1720-1904): __scene, __cam, __THREE, __renderer, __voidState, __matchState (rivals[] carries joined/surge/full/hunt/r/x/z), __setVoidR, __warpVoid, __edibles. Rival halos are RingGeometry(1.15,1.42,40) meshes added straight to the scene (rivals.ts:388,584) — a probe can read the real halo material colour per frame instead of re-deriving the rule.
- Probe written (scratchpad, not landed): cards-skeptic.mjs — one Maple match at HEAD on :4177, greedy-edible bot, per-frame samples on __matchState().t of #banner.show, every banner paint (innerHTML setter), every float (.vf go), every bubble (.vb show), and per rival the REAL halo material hex, distance, on-screen (projected through __cam). Rendering is disabled between photographed moments and the original render restored two frames before each shot. Shots: join, stuffed, surge-start, surge-red, kill (forced: __setVoidR + __warpVoid onto CHOMPZILLA), crown. Launched ~11:55 UTC under /tmp/gpu.lock.
- NOTE for anyone reading __matchState().rivals[].full: it is the LANE flag (rivals.ts:409 "past its lane: ambling", :1845), not the stuffed hunter. Stuffed = CHOMPZILLA joined && hunt dropped (as qa/_rf_hunt.mjs reads it).
- The 39% figure, closed: 589e31e (where the number first appears) touched 486 qa/ files and added qa/_mix.mjs and _mix2.mjs — those are AUDIO dBFS probes, not banner probes. `git log --all -S'one impression every 5.6'` finds only prototype3d.ts and the round-4 proposal that quotes it. There is no measurement file for 39% anywhere in history; the commit cites an inherited comment. Whether the number is right is answered by the probe below, not by the comment.
- KILL ANGLE 5, corrected: the archetype teach is NOT nowhere. rivals.ts:141/154/167/180/199 — RIVAL_VOICE[name].arch — is an in-character line for each archetype ("I am CHASING you, dear", "I go where YOU go!!", "RUNNING AWAY NOW!!", "only landmarks, darling", "slow and steady, dear"), and rivals.ts:1095 speaks one on EVERY join (`api.onSpeak?.(rv.x, rv.z, pickLine(RIVAL_VOICE[rv.name].arch), rv.name)`), right after onJoin. What gates it is prototype3d.ts:2444 `if (d < 55)` in the onSpeak handler — the join happens at the rival's turf, so the line is heard only when that turf is within 55 units. This is exactly the "bubble from the sibling itself, in its own voice, once" the commit's new comment says the teach would need if it came back — it already exists and already fires; it is distance-gagged, not unhoused. Measured join distances follow from the probe.
- Stale comment not in the diff: rivals.ts:1675 "the one character the banner promises 'CHASES you'" — that banner no longer exists.
- Welcome site at HEAD (prototype3d.ts:9301-9314): with the welcome card gone, `setTimeout(() => announce('🍽️ eat everything SMALLER than you…'), 3000)` remains. The 3000 ms (WALL clock, not tClock) existed to space the second card behind the first ("Two banner cards riding the existing queue"); it now only delays the sole instruction card by three seconds after controls go live, for no stated reason. showGuide('DRAG to move…') fires immediately on the same line, so nothing is lost, but the delay is an orphan of the deleted card.
- Kill angle 3, source half: rows (built from joined rivals only, :4251-4254) → myRank (:4255) → crown (:4278-4290), mirror (:4296-4305), "you passed" (:4310-4316), and the passer's brag bubble (:4323-4338, `if (dp < 55) bubbles.say(…, 'rival', …)`) and the end screen (:4871-4935). All intact at HEAD; the rank machine feeds every kept channel. Browser confirmation pending on the probe's paint log.

## What I ran (running notes)
- cards-skeptic.mjs maple shots — lock taken 14:59:51 UTC, load 0.55, chrome=0. Match started 33 s wall in.
- JOIN (real build, Maple): NIBBLES joined at t=11.84 with r=1.74 vs pr=1.46 (the hunter branch, rivals.ts:1083). At the shot frame t=12.04: NIBBLES d=70 on-screen=0 halo=0xff5560 (red: she is bigger); GRUMPS joined the same second at d=54 on-screen=0 halo=0x54e88a (green). BOTH halos were outside the frame when the join card would have fired. Shot: shots/cards-skeptic-join.png. The archetype join line (rivals.ts:1095) is heard only under 55 units (prototype3d.ts:2444): NIBBLES at 70 → silent; GRUMPS at 54 → spoken (bubble log to confirm at end of run).
- Run 1 (maple shots) LOST after the join shot: the probe hard-coded CHOMPZILLA as the hunter (copied from qa/_rf_hunt.mjs) but this deal's hunter was NIBBLES (h=1 at join), so the stuffed wait ran to the whistle, photographed the END screen as "stuffed" (deleted), then threw on the surge step before the sample dump. Only the join shot and its numbers above survive from run 1. Lesson recorded for the drumover-style probe: the hunter is whichever sibling __matchState().rivals[].hunt is ever true for, not a name.
- Probe fixed (hunter detected from the hunt flag; surge step guarded; samples dumped on any error) and relaunched ~15:10 UTC: maple `late` (dark from frame 1, stuffed/surge/kill/crown shots) then pirate `noshots` (natural, no forcing) in one lock hold.
- Run 2 (maple late): lock 15:10 UTC. NIBBLES (this deal's hunter) joined t=11.18. STUFFED (hunt flag dropped) at t=99.20: NIBBLES r=6.89, d=26, on-screen=1, halo=0xff5560 RED. Shot frame t=99.33 (shots/cards-skeptic-stuffed.png): NIBBLES r=6.88 d=27 on-screen, halo RED; JELLY/BIGSHOT/ECHO r≈3.5 all GREEN. At the moment the deleted card said "NIBBLES is too full — now is your chance", the only world-space signal on her says RUN. The commit's "the halo turns GOLD and pulses" is not what happens at that moment; gold needs pr > rv.r*1.05 (rivals.ts:2022), i.e. the player must first outgrow a 6.9-radius rival.
- SURGE (run 2, maple): onSurge picked JELLY at t=108.51 (d≈47, OFF-screen). Shot frame t=108.84 (+0.33 s, shots/cards-skeptic-surge-start.png): JELLY r=4.59, halo=0xff8fd0 — her OWN pink, the fair-fight fallback (rivals.ts:2029), NOT red. First red frame t=110.24 (+1.73 s after onSurge; the draft's analytic estimate was ≈2.0 s): JELLY r=5.64, d=72, OFF-screen (shots/cards-skeptic-surge-red.png). The commit's "the rival's ground halo is RED for the entire surge" is false for the first 1.7 s of this surge, and the ring the commit says onSurge fires (fx.ring at her position, :2626) went off at a rival who was outside the frame. Whole-surge on-screen fraction and red fraction come from the sample analysis at the end of the run.
- KILL (forced: __setVoidR(1.6×her r) + __warpVoid onto NIBBLES) fired at t=110.47.
- Surge frames read: in cards-skeptic-surge-start.png JELLY herself is outside the frame; what reaches the screen is her edge-clamped family bubble ("Don't make me use this") and the outer arc of a pink fx.ring at the lower-left. In cards-skeptic-surge-red.png she is still outside the frame; the red arc at the top-left corner is a halo EDGE (NIBBLES d=56 / JELLY d=72), not a ring a child can read as "that one grew bigger than you". On this surge the world-space twin never put a red ring on a visible rival during the frames photographed.
- CROWN (kill angle 3, real build): after the forced kill at t=110.47 the banner painted "👑 YOU ARE IN FRONT! BIGSHOT is behind you" at t=112.22 (paint log). The crown card still fires and names rows[1] — the rank machine feeds it. Also painted: "🚗 FIRST CAR! Tastes like vroom." t=98.5, "🏠 FIRST BUILDING! Crunch." t=100.13 (announceBeat, untouched). Shot: shots/cards-skeptic-crown.png.
- RUN 2 ANALYSIS (maple, 8004 per-frame samples, 5 halos found, end t=180.61; JSON in scratchpad cards-skeptic-maple.json):
  - BANNER: 24 paints in 180.4 s (one per 7.5 s). `#banner` carried class `show` for 176.0 s of 180.4 s = 97.6% — 0/214 samples before the first paint (t=4.62), 7790/7790 after it. Visible-card duty (2.4 s bnr animation per paint, overlaps clipped) = 52.2 s = 28.9%.
  - THE GAG IS NOT 39%, IT IS TOTAL. index.html:398 `#banner.show { animation: bnr 2.4s … forwards }` — the card fades out but the CLASS stays; the only removals are the re-paint dance (prototype3d.ts:3157) and the match-boundary reset (:6471). bubbles.ts:216 `if (ban && ban.classList.contains('show')) return;` therefore discards EVERY crowd/event/panic bubble from the first card of the match to the whistle, and bubbles.ts:358 (the 0.6 s shortener) is likewise permanently on. Measured: 29 bubbles shown all match, 27 'rival'; the only two non-family bubbles were at t=1.03 ("There are four of us. FOUR.") and t=1.65 ("Band, keep your ranks!") — BEFORE the first paint. Deleting five family cards returned ZERO crowd airtime; the town has been silent after t≈5 s of every match for as long as :216 has tested the class.
  - CROWN CARDS: 5 paints named a sibling — 21.89 s "👑 YOU ARE IN FRONT! NIBBLES is behind you", 53.09 s "👑 NIBBLES TOOK THE LEAD! get it back!", 59.04 s IN FRONT, 74.63 s TOOK THE LEAD, 112.22 s IN FRONT (BIGSHOT). Kept channel works, rank machine feeds it.
  - JOINS (4/4 off-screen at the moment of join): NIBBLES t=11.18 d=48 → first on-screen +7.4 s; ECHO t=12.33 d=186 → +9.3 s; BIGSHOT t=37.73 d=154 → +39.3 s; JELLY t=24.11 d=149 → +57.0 s. On-screen samples in the first second after each join: 0/33, 0/35, 0/37, 0/36. The archetype join line (d<55) was audible for NIBBLES only (1/4).
  - STUFFED NIBBLES t=99.02: ratio 1.56 (she is 1.56× the player), halo RED in 463/463 samples over the next 11.4 s (to my forced growth), GOLD 0/463, on-screen 176/463. Gold arrived only at +22.2 s after I had set the player to r=14.26. The card said "now is your chance" while the ring said RUN for every frame of the window measured.
  - SURGE JELLY t=108.42 (d0=45, off-screen, halo GREEN 0x54e88a at onSurge → her own pink → red): first red +1.34 s; in the 2.0 s before my forced kill, red 17/63 samples, on-screen 6/63. Clean whole-surge figures come from the natural Pirate run.
  - EATEN (natural, greedy bot) t=48.86: player r=2.74 ate Baby ECHO, ECHO on-screen=1 at the kill, float "Baby ECHO DEVOURED! +188" fired at the kill site (DOM log). Forced kill t=110.47: "Auntie NIBBLES DEVOURED! +13444". The floater twin is real: title + name + points, where the child is looking. It is absent from cards-skeptic-kill.png because `.vf.go` is a 0.9 s WALL-clock CSS animation (bubbles.ts:170-174, ends at opacity 0) and the match clock under swiftshader runs ~20× slow — a measurement trap, not a game fault; re-shoot queued with only that animation slowed.
- RUN 3 (pirate, natural, no forcing, dark throughout; 8726 samples; JSON cards-skeptic-pirate.json): lock 15:12:26 (load 5.4 — chrome=4 from the governor's just-released run; the page booted and ran cleanly, no PAGEERR).
  - BANNER: 21 paints (one per 8.6 s). `show` class on 164.0/180.4 s = 90.9%; first paint t=16.59 (a QUEST DONE beat), 0/7946 samples without it afterwards. Visible-card duty 48.6 s = 26.9%.
  - GAG, second world: 29 bubbles shown, 21 family; the 8 crowd bubbles ALL landed before the first paint (t=1.15, 1.64, 5.62, 7.04, 9.91, 11.2, 14.05, 16.2). After t=16.59: zero crowd bubbles for 164 s.
  - CROWN CARDS: 7 paints (IN FRONT ×4, TOOK THE LEAD ×3), all naming NIBBLES.
  - JOINS: 4/4 off-screen at join, 0/42, 0/47, 0/58, 0/40 on-screen samples in the first second; BIGSHOT d=72 (+1.4 s to first sight), NIBBLES d=57 (+3.8 s), ECHO d=195 (+5.5 s), GRUMPS d=332 (+136.3 s — first seen at t=169.9). Archetype join line audible (d<55): 0/4.
  - SURGE BIGSHOT (natural) t=109.04..132.00, 23.0 s: at onSurge his halo was GREEN (0x54e88a: r=3.67 vs pr=4.56 — the player could eat him), d=42, off-screen. First red +0.88 s. Red 676/983 samples (68.8%); on-screen 43/983 (4.4%); red AND on-screen 28/983 (2.8%); nearest approach 34 units. "RED for the entire surge" is false, and the red that did occur was in frame for 2.8% of the surge.
  - STUFFED NIBBLES (natural) t=99.01, ratio 1.54, d=48, off-screen, halo RED; to the whistle: red 1483 / gold 1976 / green 0 of 3932 samples, on-screen 351 (8.9%); first GOLD at t=140.3 (+41.3 s), when the player naturally outgrew her (pr 6.13 vs r 5.84). The gold twin arrives 41 s after the moment the card marked.
  - EATEN (natural) t=173.83: "Uncle BIGSHOT DEVOURED! +455" float fired. Floater twin real on Pirate too.
- RUN 4 (maple kill re-shoot, lock 15:16): stuffed t=99.18 (d=69, off-screen, RED — third confirmation); forced kill t=99.24; cards-skeptic-kill.png rewritten with the floater visible ("Auntie NIBBLES DEVOURED! +13199", plus the BUGLE ALERT stuffed news 0.06 s after onStuffed). Its crown wait timed out at t=116.8 (no rank crossing in the window; the player was already in front), so cards-skeptic-crown.png remains run 2's frame.
- RUN 4 whole-match (maple, 22 paints one per 8.2 s, class on 173.0/180.4 s = 95.9%): run4: first paint t=7.6; ban=0 after it 0/7906; visible-card duty 51.6s = 28.6%; crowd bubbles 4 (after first paint: 0); crown paints 3. Joins 5/5 off-screen (NIBBLES d=68, JELLY 155, GRUMPS 46, ECHO 105, BIGSHOT 152; 0 on-screen samples in the first second each). Stuffed t=99.01 RED (ratio 1.52); its gold at +0.2 s is the forced __setVoidR(9.89), not the game. Note: run 4 overwrote scratchpad cards-skeptic-maple.json; run 2's figures stand as recorded above.
