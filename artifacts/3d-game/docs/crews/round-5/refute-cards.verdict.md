# DRAFT — in progress (refute-cards)

## What I ran

## What I checked on disk

## Kill shots

## Corrections (verbatim)

<!-- incremental notes, 2026-09-02 ~09:50 UTC -->
## What I checked on disk (running notes)
- `git show 0efda23`: one file, src/prototype3d.ts, +28/-25. Deletions: ARCH_TAG (:2394), announceJoin call (:2404) and body (:3065-3071), the eaten card (:2462-2464), the surge card (:2626), the stuffed card (:2644), the welcome card (:9308). Nothing else. Crown cards at :4281/:4299/:4314 untouched — confirmed at HEAD.
- The 39% figure: `git log -S"39% duty"` → first appears in 589e31e (2026-08-16, "Take the unlicensed theme.mp3 out of the shipping bundle" — a commit that also dumped the whole qa/ tree). Three later comments (:2405, :4265, and the new one in onJoin) all cite it. No probe file in qa/ contains "39%" or "5.6" for the banner (grep). The figure is INHERITED, not re-measured for this commit.
- bubbles.ts:214-218 (say): `if (kind !== 'rival') { if banner.show → return; if a rival bubble active → return }`. So the gag is real for crowd/'event'/'panic' kinds; 'rival' kind is NOT gagged. float() (:296) has no banner check at all. bubbles.ts:353-361 additionally shortens live crowd bubbles to 0.6s when a banner is up.
- rivals.ts:1042-1046: a surge is picked ONLY from rivals at distance 40..200 from the player. rivals.ts:2028: halo red only when rv.r > pr*1.11; surge grows r toward surgeR at rate 0.55/s ("grow, don't pop", :1139) — so red is not instant.
- rivals.ts:2022-2024: GOLD pulsing halo requires `isHunter && !hunting && rv.r > START_R*2 && pr > rv.r*1.05` — i.e. it is the PRIZE colour (player already bigger), not a "stuffed" colour. onStuffed (:1125-1127) fires when hunting ends regardless of size.
- prototype3d.ts onJoin comment (kept by the commit): the join happens "at the rival's own turf, up to 165 units off and usually nowhere near the screen".
- `npx tsc --noEmit -p tsconfig.json` at HEAD: exit 0 (ran 2026-09-02 ~10:55 UTC).
- Card-string assertions in probes: `qa/_rf_hunt.mjs:127` looks for a BANNER matching /too full/ (falls back to the STUFFED sample if absent — `cardAt: card ? card.t : stuffed.t`), `qa/_rfbeats.mjs:73` looks for /is too full/. Both are underscore scratch probes, neither is in qa/gate.mjs (grep). No probe in qa/ or scripts/ matches "CHASES", "copies your route", "runs from everything", "only eats big", "slow and steady" (the ARCH_TAG strings) — the only "slow and steady" hit is a GRUMPS voice line in rivals.ts:199. No probe asserts on "YOU ARE IN FRONT" / "TOOK THE LEAD" / "you passed" either; the `crownprobe.mjs` the crown comment cites (:4267) is in no commit of this repo (`git log --all -- qa/crownprobe.mjs` is empty).
- `#banner .bDot` CSS (index.html:396) — its only producer was announceJoin. Now dead CSS.
- Stale comments left by the commit, all in src/prototype3d.ts: :2452 "announceFam already puts a full-screen card up for this" (no such function; the card is gone); :2473 "a full-screen card" in the ring-count list; :3062-3066 the announceJoin docblock ("A rival has arrived. Was: pink 30px text…") now sits directly above announceBeat with no function of its own; :9303 "Two banner cards riding the existing queue" (one remains); :2408-2411 "while the banner and the alert that actually tell the player were on it" (the banner no longer exists on a join).
- The crew's own bannermix log (docs/crews/round-4/hud-subtract.proposal.md:139-150, parent build): 4 banner cards in match t=0..70, 3 of them family. 4 × 2.4s / 70s = 13.7% — on THAT run the whole banner was under the 39% the commit cites.
- bubbles.ts:11 kinds are 'ambient' | 'panic' | 'event' | 'rival'. The gag at :214 covers the first three. 'event' is the HERO's own set-piece bark (prototype3d.ts:5311, positioned at voidState) — not family speech. So the commit's "crowd and event bubble" wording is right; family ('rival') bubbles were never gagged by the banner.
- Analytic (source, to be confirmed by probe): the SURGE halo. rivals.ts:1049 pins surgeR = max(rv.r, pr*1.26); :1139 eases r toward it at 0.55/s; :2028 paints red only once rv.r > pr*1.11. Starting from a rival at 0.80x the player (the softCap floor, :1088), the fraction of the gap still to close when red begins is (1.26-1.11)/(1.26-0.80)=0.326, and exp(-0.55t)=0.326 gives t≈2.0 match-seconds of a NON-red halo at the top of every surge, before the ring ever says anything. And the pick (:1042-1046) is restricted to 40..200 units from the player.
- Analytic (source): the STUFFED halo. GOLD (:2022-2024) needs `pr > rv.r*1.05`. onStuffed (:1125) fires the frame the hunt ends, when she "looms at ~1.5x whatever you are" (:1108-1111). So at the moment the deleted card said "too full — now is your chance", the halo is RED (rv.r > pr*1.11), and it turns gold only after the player has outgrown her. The card's "now" and the halo's "now" are different moments.
<!-- resumed 2026-09-02 ~11:40 UTC by a second skeptic after the first died at the session limit -->
- Re-read the diff myself (`git show 0efda23 -- src/prototype3d.ts | grep -E '^[-+]'`): the only lines removed are the ARCH_TAG table, the announceJoin call and body, the three announceHtml calls (eaten/surge/stuffed) and the welcome cardHtml call; the lines added are comments only. No logic was added. The commit message's account of the diff is accurate.
- Write-only sweep: `npx tsc --noEmit --noUnusedLocals -p tsconfig.json` at HEAD (exit 2, 70 TS6133 hits repo-wide, 19 in prototype3d.ts: makeVoidBody, applySkinToBody, HAT_MAX_W, applyHatLod, buildHat, VOID, STICKERS, WorldKey, census, lidOpen, m×2, mealBySize, pickHeadline, fillHeadline, R@4241, startT, skinGrad, stallNudged). NONE is an identifier the commit touched — every deleted card's inputs (FAMILY_TITLE :2484, esc, cardHtml, announceHtml, rivalEv.marquee) still have a reader. Nothing became write-only because of 0efda23. Kill angle 4: no kill.
- CSS at HEAD (index.html:385-396): .bIco/.bSub/.bMul still have producers in src (bMul ×1 via announceBeat, bSub ×3 via the crown cards); `.bDot` (:396) has none — its only producer was announceJoin. Dead rule, cosmetic.
- Debug surface available for the browser probe (prototype3d.ts:1720-1904): __scene, __cam, __THREE, __renderer, __voidState, __matchState (rivals[] carries joined/surge/full/hunt/r/x/z), __setVoidR, __warpVoid, __edibles. Rival halos are RingGeometry(1.15,1.42,40) meshes added straight to the scene (rivals.ts:388,584) — a probe can read the real halo material colour per frame instead of re-deriving the rule.
- Probe written (scratchpad, not landed): cards-skeptic.mjs — one Maple match at HEAD on :4177, greedy-edible bot, per-frame samples on __matchState().t of #banner.show, every banner paint (innerHTML setter), every float (.vf go), every bubble (.vb show), and per rival the REAL halo material hex, distance, on-screen (projected through __cam). Rendering is disabled between photographed moments and the original render restored two frames before each shot. Shots: join, stuffed, surge-start, surge-red, kill (forced: __setVoidR + __warpVoid onto CHOMPZILLA), crown. Launched ~11:55 UTC under /tmp/gpu.lock.
- NOTE for anyone reading __matchState().rivals[].full: it is the LANE flag (rivals.ts:409 "past its lane: ambling", :1845), not the stuffed hunter. Stuffed = CHOMPZILLA joined && hunt dropped (as qa/_rf_hunt.mjs reads it).
- The 39% figure, closed: 589e31e (where the number first appears) touched 486 qa/ files and added qa/_mix.mjs and _mix2.mjs — those are AUDIO dBFS probes, not banner probes. `git log --all -S'one impression every 5.6'` finds only prototype3d.ts and the round-4 proposal that quotes it. There is no measurement file for 39% anywhere in history; the commit cites an inherited comment. Whether the number is right is answered by the probe below, not by the comment.
- KILL ANGLE 5, corrected: the archetype teach is NOT nowhere. rivals.ts:141/154/167/180/199 — RIVAL_VOICE[name].arch — is an in-character line for each archetype ("I am CHASING you, dear", "I go where YOU go!!", "RUNNING AWAY NOW!!", "only landmarks, darling", "slow and steady, dear"), and rivals.ts:1095 speaks one on EVERY join (`api.onSpeak?.(rv.x, rv.z, pickLine(RIVAL_VOICE[rv.name].arch), rv.name)`), right after onJoin. What gates it is prototype3d.ts:2444 `if (d < 55)` in the onSpeak handler — the join happens at the rival's turf, so the line is heard only when that turf is within 55 units. This is exactly the "bubble from the sibling itself, in its own voice, once" the commit's new comment says the teach would need if it came back — it already exists and already fires; it is distance-gagged, not unhoused. Measured join distances follow from the probe.
- Stale comment not in the diff: rivals.ts:1675 "the one character the banner promises 'CHASES you'" — that banner no longer exists.
- Welcome site at HEAD (prototype3d.ts:9301-9314): with the welcome card gone, `setTimeout(() => announce('🍽️ eat everything SMALLER than you…'), 3000)` remains. The 3000 ms (WALL clock, not tClock) existed to space the second card behind the first ("Two banner cards riding the existing queue"); it now only delays the sole instruction card by three seconds after controls go live, for no stated reason. showGuide('DRAG to move…') fires immediately on the same line, so nothing is lost, but the delay is an orphan of the deleted card.
- Kill angle 3, source half: rows (built from joined rivals only, :4251-4254) → myRank (:4255) → crown (:4278-4290), mirror (:4296-4305), "you passed" (:4310-4316), and the passer's brag bubble (:4323-4338, `if (dp < 55) bubbles.say(…, 'rival', …)`) and the end screen (:4871-4935). All intact at HEAD; the rank machine feeds every kept channel. Browser confirmation pending on the probe's paint log.

## What I ran (running notes)
- cards-skeptic.mjs maple shots — lock taken 14:59:51 UTC, load 0.55, chrome=0. Match started 33 s wall in.
- JOIN (real build, Maple): NIBBLES joined at t=11.84 with r=1.74 vs pr=1.46 (the hunter branch, rivals.ts:1083). At the shot frame t=12.04: NIBBLES d=70 on-screen=0 halo=0xff5560 (red: she is bigger); GRUMPS joined the same second at d=54 on-screen=0 halo=0x54e88a (green). BOTH halos were outside the frame when the join card would have fired. Shot: shots/cards-skeptic-join.png. The archetype join line (rivals.ts:1095) is heard only under 55 units (prototype3d.ts:2444): NIBBLES at 70 → silent; GRUMPS at 54 → spoken (bubble log to confirm at end of run).
- Run 1 (maple shots) LOST after the join shot: the probe hard-coded CHOMPZILLA as the hunter (copied from qa/_rf_hunt.mjs) but this deal's hunter was NIBBLES (h=1 at join), so the stuffed wait ran to the whistle, photographed the END screen as "stuffed" (deleted), then threw on the surge step before the sample dump. Only the join shot and its numbers above survive from run 1. Lesson recorded for the drumover-style probe: the hunter is whichever sibling __matchState().rivals[].hunt is ever true for, not a name.
- Probe fixed (hunter detected from the hunt flag; surge step guarded; samples dumped on any error) and relaunched ~15:10 UTC: maple `late` (dark from frame 1, stuffed/surge/kill/crown shots) then pirate `noshots` (natural, no forcing) in one lock hold.
