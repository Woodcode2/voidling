# STUDIO — ROUND 3 (PARTIAL)

Run 2026-08-24 and STOPPED after one team: this box has four cores, so the
workflow ran two agents at a time and was starving the release gate into a
false red. TEAM STATIC finished; the other seven teams and every skeptic did
not. **Nothing here has been refuted yet.** Treat every line as a lead, not a
finding — see docs/GOVERNOR.md rule 1.

---

## TEAM STATIC — VERDICT: NO-SHIP

### What is good and must be protected

Protect these — they are the parts of my surface that are already at the bar.

**`roofKit`, `mainstreet.ts:222-243`.** The only place in the game that has internalised "the camera looks down, so the roof is the facade": AC plant scaled to `w*d/34`, vent stacks at 0.8× that count, a skylight on anything over 8×7, and a 45% chance of a roof-access hatch in `mpick([RED, BLUE, SLATE])`. Cropped at 5× (`store/03-devouring.png` 0,60 300×280) the top-left block reads as a town seen from a helicopter, not as coloured card. Every other world should be copying this, and the note above it about tar being "13.5% of the frame carrying a single grey box" is the right way to reason about this camera.

**The bathhouse eave lines, `nightmarket.ts:455-472`.** Drawing a building's shape in unlit glow because the play camera cannot see its vertical faces is the single best static idea in the repo. Measured in `store/05-lantern-bathhouse.png`: the eave line is `rgb(243,162,56)`, L=171.6, against a ground at L≈30. It is the only reason the finale is legible from anywhere in the valley. Keep every line — my finding #2 is about the fill *under* them, not the lines.

**`makeTree`'s canopy, `island.ts:3903-3912`.** One hue in three values. Measured through the shipped grade, the hue spread across a green canopy's lobes is 2–15°. In `store/03-devouring.png` at the *same* distance where the maples resolve into individual balls, the green trees resolve into a single foliage mass (crop 0,1300 520×620 at 2.4×). This is the reference the maple should be measured against, and it is already right.

**`makeMarketShed`, `nightmarket.ts:1052-1055`.** Two slopes rotated ±0.5 rad with a `VERM_D` ridge cap. The only stall roof in the game with a real ridge, and in `store/04-lantern-market.png` the sheds are the props that read as architecture while the flat stalls beside them read as slabs.

**`makeStall`'s lit back wall, `nightmarket.ts:227`.** A `G_AMBER` panel *behind* the counter so the stallholder is silhouetted against it. A lighting idea baked into a prop, at zero light cost, and it is most of why the market reads as inhabited.

**`makeLawnSign` + `signLines`' RATIO array, `mainstreet.ts:775, 796-810`.** "No two adjacent rows the same length: equal bars read as a barcode," plus a rosette. Cropped at 8× (`store/03-devouring.png` 200,1680 110×110) it reads as a fair sign at 40 device pixels. That is the fix from `0c4805b` holding up under magnification.

**The bathhouse silhouette itself, `nightmarket.ts:413-497`.** Six tiers stepping inward at 0.86, upswept corners, a lantern on every corner of every storey. Shot 05's stacked diamond is the strongest static composition in the pack.

### Findings (UNREFUTED)

#### [blocker] Every paper lantern in Lantern Night shows the camera a pure-black hole

- **Evidence:** `store/04-lantern-market.png` cropped at 8× (975,565 90×90): the lantern is a warm cream ring with a hard black octagon punched through its centre. Histogram of its disc (1000,590 30×26): **49.74% of the pixels are exactly rgb(0,0,0)**, 26% are the amber paper. `qa/_zblobs.mjs` finds 21 pure-black connected regions ≥700px in that frame's play area; the two at 889,766 (4750px) and 172,1451 (3414px) fill 83% and 73% of their own bounding boxes — solid convex shapes, not soft-edged shadows. Cause read in source: `nightmarket.ts:139` puts the top cap at `CylinderGeometry(r*0.5, r*0.62, h*0.1, 8)` in **CHAR (0x1e1e26)** at y = `h*0.98` — directly on top of the paper, which is the one face this camera sees. `qa/_zpal.mjs` confirms CHAR grades to rgb(0,0,0) at every plausible Lantern key (k=0.35→0.80). Same construction at `nightmarket.ts:172` for every lantern on a string. Call sites: `island.ts:5120, 5170, 5186, 5220, 5242, 5272` plant **338 free-standing lanterns**.
- **Why it matters:** A six-year-old is told to chase the lights. The most-repeated prop in the world — the one the file header calls "the only thing the eye is ever asked to follow" — is a cream ring with a black hole in it. At phone size it reads as a bucket, or as a missing texture. It is 338 of them, in the world whose entire identity is lanterns.
- **Proposed fix:** `nightmarket.ts:139`: shrink the cap and give it an albedo that survives — `CylinderGeometry(r*0.32, r*0.38, h*0.1, 8)` in `VERM_D` instead of `r*0.5, r*0.62` in `CHAR`. That drops the black disc from 38% to 14% of the lantern's projected area and paints what is left a red lid, which is what a real chōchin has. Same two tokens at `nightmarket.ts:172`. (`VERM_D` still needs finding #3's lift to read; do them together.)
- **Seeded draws:** Zero. `makeLantern` (`nightmarket.ts:129-143`) draws no random numbers at all, and Lantern Night scatters on `Math.random` via `island.ts:5032 plant()`, not on mainstreet's mulberry32 stream. Maple's placement is byte-identical.
- **How to measure:** `qa/blackprops.mjs`: connected components of exactly rgb(0,0,0) inside the play area (excluding the HUD bands). FAIL if any component is ≥2000 px **and** fills ≥65% of its own bounding box. The fill term is the part that matters — it is what separates a prop with no albedo (83%, 73% today) from a cast shadow (9–46% today), so the bar cannot be moved by a lighting change. Today: 2 failures in shot 04, 0 after the fix. Already written as `qa/_zblobs.mjs`.

#### [blocker] The bathhouse roof — the game's finale — renders black, and the authored fix for exactly this bought 2.5 levels of luminance

- **Evidence:** `store/05-lantern-bathhouse.png`, histogram of a roof plate (250,480 300×200): **rgb(0,0,38)**, with 88% of the region inside a 14-level band. Red and green are both exactly zero; displayed luminance is **2.5/255**, and the only thing keeping it off literal zero is the 0.46 gloss registered on TILE at `nightmarket.ts:99`. The amber eave line beside it measures L=171.6 — a **55:1 line-to-fill ratio**, so the building is a line drawing over a hole. `nightmarket.ts:419` sets `const ROOF = 0x4a5468` under a comment that says the level's TILE "renders at a luminance of about 0.05, which is to say black" and lifts it "two stops up". Simulated through the shipped grade (`qa/_zgrade.mjs`, which reproduces `prototype3d.ts:257-276` exactly and predicts the measured pixel): below k≈0.40 **TILE and ROOF are both rgb(0,0,0) — the lift buys literally nothing**; at the key solved back from the measured plate it buys L 0.0 → 2.5. The fix was reasoned, never photographed.
- **Why it matters:** This is the last thing a child eats, the thing the whole map is aimed at, and roughly 30% of the App Store screenshot for the world. From the play camera it is four black diamonds. There is no roof, no tile, no plane separation — the four hip faces of a tier land within 5 levels of each other in blue only, so you cannot tell one roof face from another except by the glow line between them.
- **Proposed fix:** `nightmarket.ts:419`: `const ROOF = 0x4a5468` → `0x6d7b96` (+48%). Measured across the whole plausible key band that lands displayed L at 29.8 (k=0.40) to 60.7 (k=0.65) — a legible dark slate that is still night, still cooler than the cedar storeys, and still 3× darker than the eave line it sits under. The response is a cliff, not a ramp: +30% already takes L from 2.5 to 38.3, so this is one token and it cannot be got at by lighting.
- **Seeded draws:** Zero — a colour constant. Lantern scatters on `Math.random`; Maple's mulberry32 stream at `mainstreet.ts:252` is untouched.
- **How to measure:** `qa/albedofloor.mjs`, static and browser-free: for each world, run every named solid in its prop palette through the same grade arithmetic at that world's key band and assert displayed luminance ≥ 8/255, with an explicit `MEANT_TO_BE_BLACK` opt-out list (CHAR, tyres). Fails today on ROOF at every k ≤ 0.6. This is the probe that would have caught the two-stop lift the day it was written, in milliseconds, without a screenshot.

#### [major] Eight of Lantern Night's seventeen solid prop colours are below the grade's black point

- **Evidence:** `nightmarket.ts:53-67` is the palette; `qa/_zpal.mjs` runs each one through the shipped grade (`prototype3d.ts:257-276`) across k = 0.35 / 0.45 / 0.59 / 0.80. **TILE (0x2e3440), TILE_D (0x222834), TIMBER_D (0x4a3324), CHAR (0x1e1e26) and WATER (0x1a3a52) render at displayed L < 1 at EVERY key in that band** — the result does not depend on which key you believe. CEDAR_D and STONE_D join them at k ≤ 0.59, plus the bathhouse ROOF (finding #2): 8 of 17. Confirmed in pixels: the prop at `store/04-lantern-market.png` 900,780 is **98.91% exactly rgb(0,0,0)** with a hard rectangular edge and no contact shadow. The header at `nightmarket.ts:46-50` states the intent — the solids are dark "because what you actually see is its silhouette against a lit ground" — but the ground in these two frames measures L≈25–45, so black-on-dark-brown has no silhouette either, and a prop that lands in a lantern pool becomes a hole punched in the picture.
- **Why it matters:** Half the prop kit in this world has no albedo on screen. A child looking at shot 04 sees warm pools of light with black rectangles sitting in them and cannot tell which black rectangles are things to eat and which are shadow. The world reads as unfinished rather than as night.
- **Proposed fix:** Raise the dark half of `nightmarket.ts:53-67` until each solid clears displayed L ≥ 8 at k=0.45. From the sweep that is roughly +35–50% per colour: TILE 0x2e3440 → 0x606d87, TILE_D → 0x4e5a70, TIMBER_D 0x4a3324 → 0x6d4c35, CEDAR_D → 0x7a5c40, STONE_D → 0x6b6b7a, WATER → 0x2c5f85. Leave CHAR black on purpose — it is ironwork and wire — but stop putting it on up-facing surfaces (that is finding #1). Note this is *consistent with* the standing HANDS OFF entry, not a challenge to it: the ledger already records that a Lantern-only light lift measured +0.4 mean luminance because the murk is albedo-bound. This is the albedo, with the arithmetic for how far it has to move.
- **Seeded draws:** Zero — colour constants only. No geometry, no `rnd()` call sites touched.
- **How to measure:** Same instrument as #2: `qa/albedofloor.mjs`, displayed-luminance floor of 8/255 per named solid per world, with an explicit opt-out list. Static arithmetic, no browser, milliseconds. Fails today on 8 of Lantern's 17 solids; the opt-out list is what keeps a deliberately-black tyre from being a false positive, and it has to be written down rather than inferred.

#### [major] The maple canopy still alternates two hues around one lobe ring, and the grade amplifies the split

- **Evidence:** `mainstreet.ts:1716` is still `i % 2 ? dark : heroDark` — JOB 5 has not landed. New measurement the board did not have: put both tones through the shipped grade (`qa/_zgrade.mjs`). For `leaf = LEAF_B` the adjacent lobes land at **rgb(141,0,0) hue 0° L 30** beside **rgb(167,91,0) hue 33° L 101** — a 33° hue step and a **3.36:1 luminance ratio between neighbouring lobes**. LEAF_D is the same (33°, 3.67:1). That is 2 of the 4 possible `mpick` outcomes, so roughly half of the 603 maple trees. Against `makeTree`, measured the same way: **≤15° hue and ≤2.2:1** across its three values. Verified in pixels — predicted rgb(141,0,0), and `store/03-devouring.png` at 700,1980 300×260 measures rgb(141–149,0,0) sitting directly against amber. The crop at 580,1830 710×620 (2×) is unambiguous: a dozen individually countable crimson/amber/salmon balls, the largest object in the bottom third of the primary gameplay screenshot. And the grade makes it worse, not better: in source hex the two tones are 27° and 1.9:1; on screen they are 33° and 3.36:1.
- **Why it matters:** Maple Falls is the world every child plays first and this is its namesake tree. At the exact distance where the green trees beside them read as foliage, these read as a bag of marbles. Two tree languages in one frame is the thing that tells a six-year-old the world was assembled rather than made.
- **Proposed fix:** `mainstreet.ts:1716`: `i % 2 ? dark : heroDark` → `dark`. `mainstreet.ts:1707`: `heroDark = shade(LEAF_HERO, 0.84)` → `shade(leaf, 0.70)` so the ring is one hue in two values, exactly as `makeTree` is. Keep `LEAF_HERO` for the crown only. Two tokens.
- **Seeded draws:** Zero. The `mpick` at :1644 and the four `mr()` per lobe at :1712-1715 are untouched, in the same order and the same ranges. Maple's placement stream stays byte-identical — which matters here specifically, because this function runs 603 times during population.
- **How to measure:** `qa/canopyhue.mjs` as the board specified it, with one correction I would insist on: measure the hue spread **after the grade**, not on the source hex. Build one `makeTree` and one `makeMapleTree` offscreen, push each lobe colour through the same `CustomToneMapping` arithmetic at that world's key, and assert the maple's post-grade hue standard deviation lands within 1.5× of the green tree's. Measuring in hex would pass a canopy that the pipeline then pulls 33° apart, which is exactly what happened to the bathhouse roof in finding #2.

#### [major] makeStall's canopy is still a flat slab with zero rise, under a comment that says "a shallow ridge"

- **Evidence:** `nightmarket.ts:214-215`: two axis-aligned `BoxGeometry` at y = `h` and y = `h-0.1` with no rotation arguments at all — rise is literally 0. In `store/04-lantern-market.png` this prop is repeated roughly a dozen times and its canopy is the largest surface in the frame after the ground; cropped at 4× (850,350 340×300) it is a featureless red rectangle floating over a counter, with the six valance bands at `:218-222` hidden underneath it from this camera. Compare `makeMarketShed` (`nightmarket.ts:1052-1055`), which rotates its two slopes ±0.5 rad and caps the ridge — those are the ones that read as buildings in the same frame. But the pitch alone is not the whole job: measured on a shed slope (430,330 160×90), **60.17% of the region is exactly rgb(100,0,0)** and the two slopes separate only 100 → 76 in red, because G and B are both zero (finding #6). The pitch buys a 1.32:1 value break and no hue information whatever.
- **Why it matters:** This camera judges a prop by its top, and a dozen of the biggest tops in the world's headline screenshot are blank parallelograms. A market stall that a child cannot tell from a table is furniture, not a stall.
- **Proposed fix:** `nightmarket.ts:214-215`: replace the two flat boxes with two pitched ones — `BoxGeometry(wD*1.15, 0.16, dD*0.66)` at `sz * -0.22` rad, one per side, plus a `VERM_D` ridge roll along the crest. That is the kit `makeMarketShed` already uses eleven lines away and `alpine.ts:159 capRoof()` uses properly. +2 parts, ~44 triangles, inside the existing merge, 0 draw calls, 0 materials. Land it together with finding #3's albedo lift or the value break will still have only one channel to move in.
- **Seeded draws:** Zero. `makeStall` draws `rnd()` three times at `:206` and one `pick()` at `:239`; adding geometry parts calls neither. Lantern scatters on `Math.random`, so Maple's seeded stream is untouched either way.
- **How to measure:** `qa/roofs.mjs` at the governor's threshold, not TEAM STATIC's: for every registered edible, isolate triangles with `normal.y > 0.7`; for any prop whose up-facing area exceeds **2** square units, report the area share of its most common vertex colour, bar 85%. Exempt props whose entire geometry is one colour. Fails today on `makeStall`, whose up-face is a single `VERM` box.

#### [major] The planned toe fix cannot restore hue on any saturated prop — and ledger #6's "one channel on the CEILING" is refuted in pixels

- **Evidence:** Not my file; flagging it because two of my own fixes are unprovable without it. Ledger #6 records the Game Day truck's flat red as "cause not yet established" and asserts "crimson's failure mode here is one channel on the CEILING." Measured: `store/06-gameday.png` at 160,2030 200×160 — the pop-up canopy renders **rgb(184,0,0)** and **rgb(151,0,0)**. Red is nowhere near 255; green and blue are exactly zero. Two channels on the floor. `qa/_zgrade.mjs` reproduces it from CRIM 0xc4342f at k≈1.2. `qa/_zgrade2.mjs` ablates the grade term by term: removing the toe at `prototype3d.ts:270` leaves CRIM at k=1.2 as **(184,0,20)** — green *still* exactly zero; removing the toe **and** the `mix(vec3(l), color, 1.07)` chroma boost at `:274` gives (180,20,31). The residual is ACES' own out-of-gamut clip at `:264`. Frame-wide, pixels with at least one channel at exactly zero (excluding pure black) run 11.05% / 17.53% / 17.85% / 41.79% across shots 03 / 06 / 04 / 05 (`qa/_zzero.mjs`).
- **Why it matters:** On my surface the consequence is that every saturated roof in the game is a one-channel surface: the only thing light can do to it is change how much red there is. No hue shift toward the sun, no sky bounce in the terminator, no desaturation at the edge. That is precisely why a pitched canopy still reads as coloured card even when the pitch works — and it is why fixes #3 and #5 have to be measured post-grade rather than in hex.
- **Proposed fix:** Not a change I am proposing — a correction to one that is already scheduled. JOB 2 replaces `prototype3d.ts:270`'s hard toe with a soft knee and gates on "no in-gamut input may produce an output channel of exactly zero." Measured, that gate would pass while CRIM still renders (184,0,20) and the truck and the canopy look identical. If the goal is hue back on saturated props, the knee has to be paired with either a chroma boost of 1.0 (`:274`) or a gamut-compress before the ACES output matrix — and the acceptance number must be a *hue* delta on a named prop colour, not a channel-zero count.
- **Seeded draws:** Zero — shader arithmetic.
- **How to measure:** Extend `qa/colorpipe.mjs` with a named-swatch table rather than a channel-floor sweep: for each world's dominant prop colours (CRIM, VERM, TEAL, GOLD, the four LEAF hues) at that world's key band, assert the rendered hue lands within 12° of the authored hue and that no channel is exactly 0. Runs as pure arithmetic against the same shader source. Fails today on CRIM, VERM, LEAF_B and LEAF_D at every key below k≈1.6.

#### [minor] Game Day's pop-up canopy presents five square units of blank up-face with no seam, ridge or batten

- **Evidence:** `tailgate.ts:322`: `ConeGeometry(2.6, 0.95, 4)` in a single `top` colour with one `GOLD` finial sphere. The bunting and the two valance boxes at `:324-326` sit at y=2.6, **below** the cone's 3.24 — so from this camera they contribute nothing. Measured in `store/06-gameday.png` (crop 100,1980 620×560 at 2×): the red canopy is a blank pyramid whose opposite facets read 184 vs 150 (a real 1.23:1 break, so the pitch is doing its job) with no break of any kind *within* a facet; the teal one beside it at 105,2360 is 98.45% a single value, rgb(0,131,110), across 18,000 pixels. In the lower half of that frame they are the two largest non-vehicle props.
- **Why it matters:** A child scanning a car park for the next thing to eat sees flat coloured lozenges. hole.io's props are simpler than this and still separate, because at that camera a silhouette needs an interior edge to be a *thing* rather than a *shape*.
- **Proposed fix:** `tailgate.ts:322`, after the cone: four thin hip battens in the valance colour along the pyramid's four hips — `BoxGeometry(0.12, 0.06, 2.7)` at ±π/4, sitting 0.02 proud of the cone. +48 triangles, 0 draw calls, 0 materials, inside the existing merge. Per art direction's conflict A this is one of six teams' "free" triangles landing in Game Day's ~4 MB of headroom, so it must be measured on Game Day before it lands there — but it is 48 triangles on a prop with a few hundred instances, not the 160k the summed proposals came to.
- **Seeded draws:** Zero. `makeCanopy` draws one `pick()` at `:313` and nothing else; adding geometry parts draws no randoms, and Game Day scatters on `Math.random`, not Maple's mulberry32 stream.
- **How to measure:** Same `qa/roofs.mjs` as finding #5, 85% single-colour share on any up-face over 2 square units. The canopy cone is ~5.4 sq units at 100% one colour today, so it fails by a wide margin and passes at roughly 78% with the battens in.

